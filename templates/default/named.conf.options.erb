options {
  <% unless node['bind']['forward_ips'].empty? -%>
    forwarders {
      <% node['bind']['forward_ips'].each do |ip| -%>
        <%= ip %>;
      <% end -%>
    };
  <% end -%>

  <% node['bind']['options'].each do |key, value| -%>
    <%= key %> <%= value %>;
  <% end -%> 
};
